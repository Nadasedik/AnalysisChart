export type scLesns= {schoolName: string, scLessons: (number | null)[];  }

type IData = {
    id: string,
    month: string,
    camp: string,
    country: string,
    school: string,
    lessons: number
  }

  export default IData